using System;
using System.Collections.Generic;
using System.Text;

namespace SocketServer
{
    //public delegate void MessageReceivedEventHandler(object sender, IMessage message);

   // public interface IClientSubscription
   // {
   //     void RegisterListener(int listenerId, ServerEvent eventCode, MessageReceivedEventHandler handler );
   //     void RegisterListener(int listenerId, ServerEvent eventeCode);
   //     IMessage GetNextMessage(int listenerId);
   //     void SendMessage(object sender, IMessage message);

   //}
}
